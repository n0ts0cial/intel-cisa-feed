name: Check CISA Vulnerabilities

on:
  workflow_dispatch:
  
jobs:
  run-script:
    runs-on: ubuntu-latest

    # Permissões necessárias para o bot do GitHub poder escrever no repositório
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Vulnerability Script
        shell: pwsh
        run: |
          # Importa e executa a função (certifique-se que a chamada da função está no fim do .ps1)
          . ./intel-cisa-feed.ps1

      - name: Commit and Push Report
        run: |
          # Configura o usuário do bot do GitHub
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # ALTERAÇÃO AQUI: Adiciona a pasta reports e os arquivos dentro dela
          git add reports/*.md
          
          # Commita se houver alterações
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Auto-generated vulnerability report for $(date +%F)"
            git push
          else
            echo "No changes to commit"
          fi
